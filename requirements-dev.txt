# TelsonBase — Developer Dependencies
# Install with: pip install -r requirements-dev.txt
# This pulls in all runtime deps and adds dev/lint/test tooling on top.

-r requirements.txt

# --- Import Sorting ---
isort==5.13.2

# --- Security Linting ---
bandit==1.9.4

# --- Dependency Audit ---
pip-audit==2.9.0

# --- Test Coverage ---
coverage==7.6.4
pytest-cov==5.0.0

# --- Type Checking (optional, run with: mypy core/ api/) ---
mypy==1.19.1

# --- Fuzz Testing (re-run: schemathesis run http://localhost:8000/openapi.json --auth-type=apikey --header 'X-API-Key: $KEY' --stateful=links) ---
schemathesis>=3.0.0
